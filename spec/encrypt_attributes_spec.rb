require 'spec_helper'

describe EncryptAttributes do
  it 'has a version number' do
    expect(EncryptAttributes::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
